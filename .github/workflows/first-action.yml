name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Messages!
        run: |
          echo "Hello world"
          echo "Goodbye cruel world!"
